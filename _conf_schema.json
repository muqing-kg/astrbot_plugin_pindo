{
  "default_brand": {
    "description": "默认品牌色板",
    "type": "string",
    "options": ["MARD", "COCO", "漫漫", "盼盼", "咪小窝", "Hama", "Perler", "Artkal S"],
    "default": "MARD",
    "hint": "「拼豆」不带品牌参数时使用的色板"
  },
  "robot_watermark": {
    "description": "机器人名水印",
    "type": "string",
    "default": "",
    "hint": "显示在图纸底部信息条开头，留空则不显示"
  },
  "wait_timeout": {
    "description": "补图等待时长（秒）",
    "type": "int",
    "default": 30,
    "hint": "「拼豆」未带图时等待发送人补图的窗口，超时静默结束"
  },
  "max_long_edge": {
    "description": "图纸最长边颗数上限",
    "type": "int",
    "default": 120,
    "hint": "不锁比例，默认宽 35 颗；任一边超过此值时等比压缩到该值"
  },
  "enable_site": {
    "description": "启用内置 WebUI（Pindo 网页版）",
    "type": "bool",
    "default": true,
    "hint": "由插件自带 HTTP 服务托管，修改端口后需重载插件生效"
  },
  "site_host": {
    "description": "WebUI 监听地址",
    "type": "string",
    "default": "0.0.0.0",
    "hint": "对外提供访问用 0.0.0.0；仅本机访问改 127.0.0.1"
  },
  "site_port": {
    "description": "WebUI 端口",
    "type": "int",
    "default": 8765,
    "hint": "独立于 AstrBot 面板（6185）；公网访问需在防火墙/安全组放行该端口"
  },
  "site_title": {
    "description": "网站标题",
    "type": "string",
    "default": "",
    "hint": "留空使用默认标题「Pindo 拼豆图纸生成器」；修改后需重载插件"
  },
  "site_url": {
    "description": "外部访问网站地址",
    "type": "string",
    "default": "",
    "hint": "填写 WebUI 的公网地址（如 http://服务器IP:8765）；出图提示文案使用该地址"
  },
  "send_site_hint": {
    "description": "出图时附带网站提示",
    "type": "bool",
    "default": false,
    "hint": "开启后出图会附带下方自定义文字（渲染后为空则不发送）"
  },
  "site_hint_text": {
    "description": "出图附带文字模板",
    "type": "string",
    "default": "完整功能请前往 {url}",
    "hint": "占位符 {url} 会替换为上方「外部访问网站地址」；不含占位符则原样发送；渲染后为空则不发送"
  }
}
